/**
 * Pan + zoom for SVG scenes — now provided by the shared framework
 * package (extracted from this app verbatim). Re-exported here so the
 * graph components keep their historical import path.
 */
export {
  usePanZoom,
  type PanZoomTransform,
  type UsePanZoomResult,
} from '@criblio/app-utils/graph';
