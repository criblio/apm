/**
 * Compact SVG sparkline — now the shared implementation from
 * @cribl/app-utils/viz (moved there verbatim). Re-exported to keep existing
 * import sites unchanged.
 */
export { Sparkline as default } from '@cribl/app-utils/viz';
